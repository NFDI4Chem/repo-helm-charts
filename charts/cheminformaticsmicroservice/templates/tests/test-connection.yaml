apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cheminformaticsmicroservice.fullname" . }}-test-connection"
  labels:
    {{- include "cheminformaticsmicroservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cheminformaticsmicroservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
